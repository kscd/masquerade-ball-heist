extends Node

# No logic needed here, just the definitions
signal object_clicked(object_node)

signal lives_changed(current_lives)

signal round_over(condition: Player.Round_end_condition)

signal game_over
